const fs = require('fs');
// fs.readFile
// fs.writeFile

// how we are storing the state of our database

// create a class called store

// add method called 'read'

// returns the json file being read

// use readfile

// write is going to take the notes in as a parameter
// that is going to write the note to the json

// next method: get notes

// return what the notes are 

// another method: add note
// this will take in the notes as a parameter 
// find a way to get the current notes and add the new note to it

// remove note
// this takes in a parameter of the note id - uses filter function
// filter out notes and creates a new array


// module.exports = new Store();